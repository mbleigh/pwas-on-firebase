{
  "name": "firebase-shell-caching",
  "version": "0.0.1",
  "description": "An example of how to cache an app shell with Firebase",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist; mkdir dist && cp -r public/* dist && node build.js"
  },
  "author": "Michael Bleigh",
  "license": "Apache-2.0",
  "devDependencies": {
    "glob": "^7.0.3",
    "sw-precache": "^3.1.1"
  }
}
